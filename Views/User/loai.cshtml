
@foreach (var item in Model)
{
    <ul class="no-bullets">
        <li>
            <a href="@Url.Action("SPTheoloai",new { id = item.MALOAI })" class="no-bullets" style="color: #103a78"> @item.TENLOAI </a>
        </li>
    </ul>
}

